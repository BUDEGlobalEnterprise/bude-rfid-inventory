# Shared

Cross-language artifacts shared by both the mobile app and the backend — schemas, generated types, OpenAPI specs, etc.

Empty in Phase 1. Add subfolders as concrete sharing needs emerge (e.g. `openapi/`, `proto/`).
