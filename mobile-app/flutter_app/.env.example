# Flutter-specific environment hints.
# These are NOT loaded automatically — they document what `Environment` in
# lib/core/config/environment.dart expects. Wire to flutter_dotenv if needed later.

API_BASE_URL=http://localhost:8000
APP_NAME=Bude Inventory (Dev)
IS_PRODUCTION=false
