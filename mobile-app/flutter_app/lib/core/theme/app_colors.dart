import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Brand primaries
  static const Color primary = Color(0xFF0057B7); // Bude blue
  static const Color secondary = Color(0xFF00A86B); // warehouse green
  static const Color tertiary = Color(0xFFF5A623); // amber accent

  // Neutral surface tokens
  static const Color surface = Color(0xFFF8F9FB);
  static const Color surfaceDark = Color(0xFF121820);

  // Status colours
  static const Color error = Color(0xFFD32F2F);
  static const Color warning = Color(0xFFF9A825);
  static const Color success = Color(0xFF2E7D32);
  static const Color info = Color(0xFF0288D1);
}
