# Deployment

Phase 1 placeholder. Real deployment guides land in Phase 2 once the first end-to-end flow exists.

## Planned topics

- Frappe bench setup (production-mode, Nginx, supervisor)
- Installing `bude_api` onto a site
- Flutter release builds per platform
  - Android: `flutter build appbundle`
  - iOS: `flutter build ipa`
  - Windows: `flutter build windows`
  - Web: `flutter build web`
- CI artifacts and release tagging
- Backup and restore for ERPNext data
