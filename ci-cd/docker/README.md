# Docker

Phase 1 placeholders. Real images get built in Phase 2 once the first end-to-end flow is working.

| File | Purpose |
|---|---|
| `Dockerfile.backend` | Builds a Frappe bench image with `bude_api` installed |
| `docker-compose.dev.yml` | Local dev stack: Frappe + MariaDB + Redis |
| `.dockerignore` | Excludes Flutter build artifacts, `.git`, etc. |

These files are stubs — they are not yet runnable.
